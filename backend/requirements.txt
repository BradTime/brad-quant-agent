# Web framework
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv

# HTTP / data utils
httpx
pandas
backtrader==1.9.78.123

# Database
SQLAlchemy
psycopg2-binary
alembic

# A 股数据源（DataProvider 实现）
akshare
baostock
efinance

# AI（DeepSeek 为 OpenAI 兼容协议）
openai

# RAG：向量检索（pgvector）+ 本地中文 embedding（默认 bge-small-zh）
pgvector
sentence-transformers

# 多智能体编排（盘前早报）+ 可观测（LangSmith 随 langchain 附带）
langgraph
langchain-openai

# Auth
python-jose[cryptography]
passlib[bcrypt]

# 行情拉取调度器
APScheduler

# 可观测（Sentry，DSN 未配置则不启用、不引入运行开销）
sentry-sdk

# 测试 / 质量
pytest
ruff

# 后置（按需启用）：
# redis
